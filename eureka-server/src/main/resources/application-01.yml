server:
  port: 8801
spring:
  application:
    name: eurekaserver
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8801/eureka